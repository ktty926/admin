<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- mybatis框架只有接口 没有实现类
     mybatis的映射文件就相当于是接口的实现类
               主要用来编写sql语句
     mapper 需要与映射接口对应
     namespace 配置类的全限定名
 -->
<mapper namespace="com.jk.mapper.FinancingMapper">
    <select id="getCount" resultType="Long">
        select count(1) from Withdrawal where 1=1
        <if test="shzt==null or shzt==0">
            and 2=2
        </if>
        <if test="shzt==1">
            and examineStatus=1
        </if>
        <if test="shzt==2">
            and examineStatus=2
        </if>
        <if test="mindate!=null and mindate!=''">
            and applyDate &gt;=str_to_date(#{mindate}, '%Y-%m-%d')
        </if>
        <if test="maxdate!=null and maxdate!=''">
            and applyDate&lt;=str_to_date(#{maxdate}, '%Y-%m-%d')
        </if>
    </select>
    <select id="findWithdrawat" resultType="com.jk.model.Withdrawal">
        select * from Withdrawal where 1=1
        <if test="shzt==null or shzt==0">
        and 2=2
        </if>
        <if test="shzt==1">
        and examineStatus=1
        </if>
        <if test="shzt==2">
        and examineStatus=2
        </if>
        <if test="mindate!=null and mindate!=''">
            and applyDate &gt;=str_to_date(#{mindate}, '%Y-%m-%d')
        </if>
        <if test="maxdate!=null and maxdate!=''">
            and applyDate&lt;=str_to_date(#{maxdate}, '%Y-%m-%d')
        </if>
        order by applyDate
        limit #{page},#{limit}

    </select>
</mapper>